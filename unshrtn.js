// Generated by CoffeeScript 1.6.2
(function() {
  var EmptyWriter, app, db, express, jar, level, request, stream, unshorten, userAgent, _ref,
    __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  level = require('level');

  stream = require('stream');

  express = require('express');

  request = require('request');

  EmptyWriter = (function(_super) {
    __extends(EmptyWriter, _super);

    function EmptyWriter() {
      _ref = EmptyWriter.__super__.constructor.apply(this, arguments);
      return _ref;
    }

    EmptyWriter.prototype._write = function(b) {};

    EmptyWriter.prototype._transform = function(x) {
      return this.end();
    };

    return EmptyWriter;

  })(stream.Writable);

  app = express();

  app.set('json spaces', 2);

  userAgent = "Mozilla/5.0 (Windows NT 6.1; WOW64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/36.0.1985.67 Safari/537.36";

  db = level('./unshrtndb');

  jar = request.jar();

  process.setMaxListeners(10);

  app.get("/", function(req, res) {
    var short;

    short = req.query.url;
    if (short) {
      return unshorten(short, db, function(error, long) {
        if (error) {
          return res.status(500).json({
            short: short,
            long: null,
            error: error
          });
        } else {
          console.log("" + short + " -> " + long);
          return res.json({
            short: short,
            long: long
          });
        }
      });
    } else {
      return res.status(400).json({
        short: null,
        long: null,
        error: "please supply url query parameter"
      });
    }
  });

  unshorten = function(short, db, callback) {
    if (short === null) {
      callback("short url cannot be null", null);
      return;
    }
    short = String(short);
    return db.get(short, function(err, long) {
      var error, opts, r, w;

      if (!err) {
        return callback(null, long);
      } else {
        try {
          opts = {
            url: short,
            jar: jar,
            timeout: 10000,
            headers: {
              "User-Agent": userAgent
            }
          };
          w = new EmptyWriter;
          r = request(opts);
          r.on('response', function(resp) {
            if (resp.statusCode !== 200) {
              return callback("HTTP " + resp.statusCode, null);
            } else {
              long = String(resp.request.uri.href);
              db.put(short, String(long));
              return callback(null, long);
            }
          });
          return r.pipe(w);
        } catch (_error) {
          error = _error;
          return callback(error, null);
        }
      }
    });
  };

  if (require.main === module) {
    app.listen(3000);
  }

  exports.unshorten = unshorten;

}).call(this);
